//hook pque se encargue de realizar cualquier petición a una API
// https://api.themoviedb.org/3/movie/11

export const useFetch = () =>{
    // estado par guardar la data
    const [data, setData] = useState([]);
}